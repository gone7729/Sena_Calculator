# coord Discord Bot 의존성 (Phase 6)
# 설치: pip install -r bot/requirements.txt  (또는 uv pip install)

discord.py>=2.3.0
PyYAML>=6.0
python-dotenv>=1.0.0
anthropic>=0.40.0   # v0.16: Option C — @bot <text> 자연어 대화 (ANTHROPIC_API_KEY 필요)

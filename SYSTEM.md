# 세나리 시뮬레이터 — 시스템 정리 (단일 기준 문서)

> **이 문서가 데미지 계산식과 시뮬레이션 게임 시스템의 단일 기준(Single Source of Truth)이다.**
> 2026-06-10 코드 전수검수 기준으로 작성. 흩어져 있던 md(damage_test_results, system_update,
> .claude/CLAUDE_MEMORY, docs/* 일부)의 시스템 서술은 본 문서로 통합됐다. 충돌 시 **코드 > 본 문서 > 구 문서** 순.
> 수치·규칙이 바뀌면 코드와 함께 이 문서를 갱신할 것.

---

## 목차

1. [프로젝트 구조](#1-프로젝트-구조)
2. [스탯 계산 (StatCalculator)](#2-스탯-계산-statcalculator)
3. [버프/디버프 병합 규칙](#3-버프디버프-병합-규칙)
4. [효과 시스템 (Effects / 레거시 이중 구조)](#4-효과-시스템)
5. [데미지 계산 파이프라인 (DamageCalculator)](#5-데미지-계산-파이프라인-damagecalculator)
6. [상태이상 시스템](#6-상태이상-시스템)
7. [메인 배틀 시뮬레이터 (단일 보스전)](#7-메인-배틀-시뮬레이터)
8. [공성전 시뮬레이터 (SiegeBattleSimulator)](#8-공성전-시뮬레이터)
9. [옵티마이저](#9-옵티마이저)
10. [데이터 DB 구조](#10-데이터-db-구조)
11. [도구(tools)와 웹 연동](#11-도구tools와-웹-연동)
12. [검증 이력과 알려진 한계](#12-검증-이력과-알려진-한계)
13. [부록: 구 문서 이관표](#13-부록-구-문서-이관표)

---

## 1. 프로젝트 구조

WPF 앱은 폐기 방향(웹 전환). **DB/ + Models/ + Services/** 가 공유 코어이고, `tools/` 의 각 콘솔 앱과
`tools/SiegeApi`(웹 백엔드)가 이 코어를 직접 `Compile Include` 한다. 코어 수정 = 웹 자동 반영.

```
DB/        캐릭터·적·장비·장신구·전용무기·펫·공성요일·상태이상 데이터
Models/    Character/Skill/Passive/BuffSet/DebuffSet/Enemy/Stage/Equipment/Pet 등
Services/
  DamageCalculator.cs        데미지 계산 (보스→아군 받피해 계산에도 재사용)
  StatCalculator.cs          스탯 계산 (장비/세트/초월/진형/펫/버프)
  EffectManager.cs           효과 집계 단일 창구 (구 BuffCalculator 대체)
  EffectConverter.cs         Skill/Passive/Pet → BattleEffect 변환
  PerDebuffBonusExtractor.cs 「적 디버프 1개당 피증」 추출
  BattleEngine/
    BattleSimulator.cs       메인(단일 보스전) 시뮬
    TurnManager.cs           턴 큐 생성
    SiegeBattleSimulator.cs  공성전 시뮬 (핵심, 최대 파일)
    SiegeOptimizer.cs        공성전 팀·진형·자리·기어·로테 탐색
    RotationBeamSearch.cs    스킬 로테이션 빔서치
    SurvivalResolver.cs      치사 피해 생존 판정 (부활/권능/불굴/불사)
  Optimizer/
    EquipmentOptimizer.cs    장비 최적화 (단일 캐릭 + 공성용 Fast 경로)
tools/     검증·배치·API 콘솔 (11절)
web/       Next.js 16 사이트 (BeHumble). 데이터는 SenaDataExport 산출 JSON
```

---

## 2. 스탯 계산 (StatCalculator)

### 2.1 합산 순서

`Services/StatCalculator.cs` `Calculate()` 기준.

**① 고정(flat) 스탯 합산**

```
flatAtk = 장비기본(무기×2) + 잠재 + 장비부옵 + 펫기본 + 장비메인 + 패시브FlatBonus + 전용무기(247, 타입별)
flatDef = 장비기본(방어구×2) + 잠재 + 장비부옵 + 펫기본 + 장비메인 + 패시브FlatBonus
flatHp  = 장비기본(방어구) + 잠재 + 장비부옵 + 펫기본 + 장비메인 + 패시브FlatBonus
```

**② 스탯창 % 합산** — *진형%·펫잠재%는 여기서 제외*

```
totalAtkRate = 초월% + 세트% + 장비부옵% + 장신구% + 장비메인% + 전용무기조율%
(Def/Hp 동일. Hp는 펫잠재 HP%가 스탯창에 포함됨 — Atk/Def와 다름에 주의)
```

**③ 기본 스탯(스탯창 표시값)**

```
baseStat = 캐릭기초 × (1 + totalRate/100) + flat
```

**④ 별도 보너스** — 진형%·펫잠재(공/방)%는 **기초 스탯(장비 적용 전)에만** 곱한다

```
separateBonus = 캐릭기초 × (진형% + 펫잠재%) / 100
```

- 진형: `FormationBonus.Atk_Rate_Back`(후열), `Def_Rate_Front`(전열) — `DB/BasicStatDB.cs`
- 이 모델은 실측 검증 완료 (DmgCheck 보스 1타 오차 ~1%, 12절)

**⑤ StatScaling(스탯 비례 증가)** — 패시브 `StatScalings`

```
bonus = min( floor(기준스탯 / SourceUnit) × PerUnit, MaxValue )
```

**⑥ 버프 적용 (최종)**

```
FinalAtk = (baseStatAtk + separateBonusAtk) × (1 + (상시 + 턴제 + 펫버프)% / 100)
```

상시/턴제/펫의 결합은 **합산 후 1회 곱연산**이다 (각각 따로 곱하지 않음. 2026-02-06 엑셀 대조로 확정).

### 2.2 물리/마법 공격력 분리

- `Character.AttackType`(Physical/Magic), `BaseStatSet.Atk` / `MagicAtk`, `BuffSet.Atk_Rate` / `MagicAtk_Rate`
- `EffAtkRate(buff, atkType)` 헬퍼가 캐릭터 타입에 맞는 필드만 읽는다.
  **마법형 캐릭터의 "공격력 증가" 버프는 반드시 `MagicAtk_Rate`로 선언** — `Atk_Rate`로 넣으면 0으로 읽혀 무효.
- 장비/옵션 데이터는 단일 `Atk` 표기 — 착용자 타입이 의미를 결정(분리하지 않음, 의도된 설계).
- 전용무기는 `IsMagic` 플래그로 Atk/MagicAtk 중 하나에만 flat 적용.

### 2.3 캐릭터 기본 스탯·초월

- 기초 스탯: 등급(전설/희귀) × 타입(공격/마법/만능/지원/방어)별 테이블 — `DB/BasicStatDB.cs`. 캐릭터 선언에는 스탯 미포함.
- 초월(1~12): 1~6은 `TranscendType`별 고유 보너스, 7~12는 공용 보너스. 레벨 누적 가산.

---

## 3. 버프/디버프 병합 규칙

게임 룰(인게임 검증) = 코드(`EffectManager.cs`) 동작. **디버프도 버프와 완전히 동일한 규칙.**

### 3.1 4카테고리

| 카테고리 | 구성 | 카테고리 내 | 카테고리 간 |
|---|---|---|---|
| 상시 | 패시브 자버프 + 패시브 파티버프 | **MaxMerge** | **Add** |
| 턴제 | 조건부 + 액티브(스킬) 자/파티 버프 | **MaxMerge** | **Add** |
| 펫 | 펫 스킬 버프 | 항상 Add | **Add** |
| 진형 | 진형 보너스 | — | 별도 채널 (2.1절 ④의 별도보너스) |

- "같은 종류" = `BuffSet`/`DebuffSet` **필드 단위**. 게임 내부 분류명이 다르면 별도 필드로 분리되어 자동 Add
  (예: `Mark_Purify`, `Mark_Energeia`). 이름이 달라도 같은 분류면 같은 필드로 선언 (예: 여포 「용이무모」 = `Atk_Rate`).
- 예: 상시 방깎20 + 턴제 방깎36 = **56** (간 Add) / 턴제 방깎29 + 턴제 방깎36 = **36** (내 MaxMerge)
- 적에게 거는 디버프는 시뮬에서 `AddEnemyDebuff(category)` 로 카테고리를 **명시**해야 한다
  (출처별 카테고리 구분 없이 합산하던 옛 버그 정정됨). 같은 출처 재적용은 `RemoveBySource` 후 재등록(1회만 유지).

### 3.2 버프 해제 / 디버프 해제

- **버프 해제(적 버프 제거)**: 턴제 버프만 대상(상시 불가). **적용 순서대로(FIFO)** 제거 — `BattleEffect.ApplyOrderId`.
  피해 면역·피해 무효화·권능은 턴제여도 해제 불가(건너뜀).
- **디버프 해제(cleanse)**: `DebuffCleanse` + `DispelDebuffCount` — 아군 디버프 N개 제거. 공성 시뮬 구현됨.
- 적의 보호막(루디)·적군 버프(챈슬러→스파이크)도 아군의 버프 해제로 제거 가능.

### 3.3 턴 경과

- `EffectManager.TickTurn()`: 비영구 효과 `RemainingTurns--`, 0이면 제거.
- 차감 시점: **행동자(이펙트 보유자)가 턴 소모 행동(평타)을 할 때** (per-character-action 모델).
  스킬턴(0턴)에는 버프/디버프/DoT 턴이 줄지 않는다(쿨다운만 감소).

---

## 4. 효과 시스템

### 4.1 이중 구조 (신규 Effects 우선, 레거시 폴백)

```csharp
// 신규 (CharacterDB 전 캐릭터가 이 방식으로 통일됨)
Skill.LevelData.Effects   = List<SkillEffect>      // 턴제: Buff/Debuff/StatusAilment/...
Passive.LevelData.Effects = List<PersistentEffect> // 지속: Buff/Debuff/MarkAttack/...

// 레거시 필드 (SelfBuff/PartyBuff/DebuffEffect/StatusEffects 등) — Effects가 비어 있을 때만 사용
```

`EffectConverter.FromPassive/FromSkill/FromPet`이 BattleEffect로 변환. `Skill.Bonus`(스킬 종속 스탯)는
효과 리스트와 별개로 유지된다.

### 4.2 BattleEffect로 변환되지 않는 특수 메카닉 (별도 채널)

| 타입 | 소비처 |
|---|---|
| CoopAttack(협공), MarkAttack(표식) | DamageCalculator 직접 |
| StatScaling, FlatBonus | StatCalculator 직접 |
| PerEnemyDebuffDmgBonus | PerDebuffBonusExtractor |
| Revival/Authority/불굴/불사(생존기) | SurvivalResolver |
| Immunity, DamageNullification, BuffDispel, DebuffCleanse, TriggeredHeal/SkillCast/FixedDamage, CooldownReset, FocusTarget, PainEndurance 등 | 시뮬 런타임 (일부는 미구현 — 12.3절) |

### 4.3 초월 병합

- 패시브: `SelfBuff/PartyBuff`는 **Override**(0이 아닌 필드만 최종값으로 대체), `Debuff`·조건부는 **Add**, `Effects`는 누적.
- 스킬(`GetTranscendBonus`): `Bonus` Override, `Debuff`·`ConsumeExtra` Add, `TargetCount/AtkCount/Cooldown` 오버라이드, `Effects` 누적.
- `GetEffectiveEffects`: 초월 Effect는 base에서 (Target, Type, StatusType)이 같은 항목을 찾아
  **필드별 Override** (예: 방깎 34→41). 없으면 추가.
  ⚠ 상태이상 확률 상승형 초월("기절 55→65%")이 base+초월 이중 적용될 수 있는 데이터가 일부 남아 있음 — 12.3절.

### 4.4 스킬 티어 폴백

`GetTargetCount / GetAtkCount / GetCooldown`: **초월 오버라이드 > 레벨별(기본/강화) > Skill 기본값**.
강화 레벨 쿨이 0(데이터 버그)이면 기본 레벨로 폴백.

---

## 5. 데미지 계산 파이프라인 (DamageCalculator)

`Services/DamageCalculator.cs` `Calculate()`. 모든 단계는 코드와 1:1 대조 완료.

### 5.1 단계별 공식

```
[1] PreCast 버프    : preCast공% > 입력 공버프% 이면 차분만 추가 적용
                      FinalAtk ×= 1 + (preCast% − 입력공버프%)/100
[2] 타수            : 초월오버라이드 > 레벨별 > 기본
[3] 스킬배율        : Ratio / 100
[4] 방어관통        : min(입력방무 + 스킬방무, 100%)
[5] 방어계수        : 1 + 적방어 × max(1 + (방증−방깎)/100, 0) × (1−방무) / 467
[6] 치명계수        : 발동 시 치피/100, 미발동 1.0
                      기댓값 모드: 1 + pc×(치피/100 − 1),  pc = clamp(치확/100, 0..1)
[7] 약점계수        : 동일 구조 (약피, 약확)
[8] 피증계수(합연산): 1 + (기본피증 + 타입피증 + 조건부피증 + 디버프당피증 + 보스피증 + n인기피증 − 피감) / 100
[9] 잃은HP 보너스   : DamageMultiplier·SkillDmgMultiplier 양쪽에 곱 (LostHpMultiplier로 별도 보관)
[10] 기본피해       : (공/방계수 × 배율 [+ 방어·HP비례항]) × 치명 × 약점 × SkillDmgMultiplier + 고정피해
[11] 조건부 추가    : 공/방계수 × 조건부% × SkillDmgMultiplier × 치명 × 약점  (PerHit이면 ×타수)
                      + 시전자최대HP × % × 치명 × 약점 (SelfHpRatio형)
[12] HP비례 피해    : 대상 최대/현재HP × % (AtkCap = FinalAtk×%로 상한) × 피증 × 치명 × 약점
[13] 스택소모 추가  : (HP비례항 + 공격력비례항) × (피증 ÷ 잃은HP배수) × 치명 × 약점   ← 잃은HP 제외
[14] 1타 합계       : 기본 + 조건부 + 약점추가 + 치명추가
[15] 블록           : 1타·부속 피해 전부 × 0.5
[16] 축복           : 1타 상한 = 대상HP × 축복%
[17] 최종           : (1타 × 타수 + HP비례 + 스택소모) × (1 + (취약 + 보스취약 + 받피증)/100)
```

**피증은 합연산이다.** (damage_test_results.md의 "곱연산 채택" 기록은 2026-01-29 시점의 폐기된 실험.
2026-02-06 인게임·엑셀 재검증으로 합연산 확정, 이후 모든 검증이 합연산으로 통과.)

### 5.2 보조 채널

- **약점 추가피해**: `공/방계수 × WekBonusDmg% × SkillDmg × 치명 × 약점계수` (기댓값 모드에선 약점계수 자리에 `pw×약피` — 발동 시에만 존재하는 피해이므로 확률 가중). `PerHit`이면 ×타수. **치명 추가피해** 대칭.
- **협공(CoopAttack)·표식(MarkAttack)**: `공/방계수 × Ratio% × 피증 × AtkCount` (+ 대상최대HP비례항).
  **치명·약점·취약 미적용.**
- **회복**: `FinalAtk/Def/Hp × Heal*Ratio%` 중 하나 × (1 − 회복감소%). 피해비례 회복(`HealDmgRatio`)은 별도.
- **디버프당 피증**: 패시브/스킬의 `PerEnemyDebuffDmgBonus`를 추출(Max), `min(대상 디버프 수, MaxStacks) × %`를 [8]에 가산.

### 5.3 배율 3종

| 배율 | 내용 | 사용처 |
|---|---|---|
| `DamageMultiplier` | 피증 전체 (타입피증 포함, 잃은HP 포함) | HP비례, 협공, 표식, 상태이상 |
| `SkillDmgMultiplier` | 스택소모 스킬일 때 **자버프 타입피증 제외** | 기본피해, 조건부, 약/치 추가 |
| `LostHpMultiplier` | 잃은HP 배수만 분리 보관 | 스택소모에서 나누어 제거 |

### 5.4 계수 적용 범위 요약

| | 방어계수 | 배율 | 치명 | 약점 | 피증 | 잃은HP | 취약(최종) |
|---|---|---|---|---|---|---|---|
| 기본피해 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 조건부 추가 | ✓ | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| HP비례 | — | — | ✓ | ✓ | ✓ | ✓ | ✓ |
| 스택소모 | △(공격력항만) | — | ✓ | ✓ | ✓ | **✗** | ✓ |
| 협공/표식 | ✓ | — | ✗ | ✗ | ✓ | ✗ | ✗ |

### 5.5 잃은HP 보너스 우선순위

```
① LostHpActualRemainingPct 입력 시(시뮬): 보너스 = Max × (100 − 실제잔여%) / 100
② LostHpAssumedRemaining > 0 (스킬 데이터): 보너스 = Max × (100 − 가정잔여%) / 100
③ 둘 다 없으면: 보너스 = Max (최대 가정)
```

---

## 6. 상태이상 시스템

`DB/StatusEffect.cs`(데이터) + `Models/StatusEffectType.cs`(enum).

### 6.1 분류

- **CC(행동 불가)**: 기절, 침묵, 빙결, 석화, 빙극, 마비, 감전, 수면(최대HP 7% 피해 시 각성), 혼란, 진탕, 공포, 실명, 도발, 영멸(생존기 봉인)
- **DoT**: 화상(공80%), 출혈(공60%), 중독(최대HP 6%), 마력역류(최대HP 12%, 최대 7중첩, 공격력 90% 상한), 즉사(현재HP 20%/턴 · 3턴 · 만료 턴 사망 · 2중첩 시 즉시 사망), 사슬피해
- **특수**: 폭탄(만료 시 120% 방무40%, 최대 3중첩), 폭파/출혈폭발(소모형), 수정결정(피격 N회마다 고정 2435), 빗나감, 회복불가, 생명력 전환, 재생, 위장(1인 공격 비대상), 매의발톱(EagleClaw 스택)
- 기본 지속 2턴(개별 정의 우선).

### 6.2 적중 확률·기대 스택 (계산기)

```
적중확률 = clamp( (Chance/100) × (1 + (효과적중 − max(0, 효과저항 − 효저감소))/100), 0..1 )
기대스택 = min( Σ(타수 × 스택/타 × 적중), MaxStacks )       // ForceStatusEffect = 무롤 확정
피해     = (공/방계수×AtkRatio + HP비례항(+AtkCap상한) + 고정) × 스택 × DamageMultiplier
```

base + 초월 상태이상은 Type 기준 dedup(초월이 덮어씀).

### 6.3 면역

`StatusImmunityTurns[Type]` — 면역 캐릭터의 평타/패시브가 파티에 턴제 면역 부여(Max 갱신).
면역 캐릭터가 죽거나 행동하지 못하면 면역이 끊긴다. 즉사 면역(샤오)도 이 일반 메커니즘으로 구현.
`IgnoresTurnDamageImmunity`(관통) 스킬은 적 피해면역을 무시.

---

## 7. 메인 배틀 시뮬레이터

`Services/BattleEngine/BattleSimulator.cs` + `TurnManager.cs`. 단일 보스전용.

### 7.1 턴 구조

```
배틀 시작(0턴): 선공 측 스킬 (턴 소모 없음)
→ 후공 기본공격 2회(1~2턴) + 후공 스킬
→ 선공 기본공격 2회(3~4턴) + 선공 스킬
→ 반복
```

- 선공: 아군 총 속공 합 ≥ 보스 속공 → 아군 선공.
- 기본공격 순서: 파티 내 속공 내림차순 순환(동률 PartyIndex). 사망자는 스킵하고 다음 순번이 그 턴을 가짐.
- 스킬 로테이션: 유저 지정(`UserDefined`) / 자동(궁극기→스킬4→3→2→1 반복).

### 7.2 시간·쿨다운

- 쿨다운은 **초 단위**, 행동 소요시간만큼 전체 감소. DoT 처리 시 +1초.
- **상대(보스) 스킬 사용 시 아군 전체 쿨 5초 감소** — 단 잔여 5초 이하인 스킬엔 미적용.
- 자기 스킬 쿨은 시전 완료 후 설정(자기 컷신 시간에 자기 쿨 안 깎임).

### 7.3 받는 피해 / PainEndurance(트루드 「전투의 희열」)

```
ApplyIncomingDamage:
  ① 피해 무효화(피격 N회/N턴, 물·마 한정) 확인 → 무효 시 0
  ② PainEndurance: 직접 피해 ≥ 최대HP × Threshold% 시 발동
     즉시 (100−R)% 적용 + R%를 Duration턴 분산 (독립 큐로 누적 가능)
  ③ 분산분은 **다음 본인 평타 차례 시작 시** 1회 처리 후 평타
  ④ 치사 피해 → SurvivalResolver (영멸 체크 → 불굴/불사 → 권능 → 부활 → 사망)
```

---

## 8. 공성전 시뮬레이터

`Services/BattleEngine/SiegeBattleSimulator.cs`. 요일별(월~일) 공성전.

### 8.1 기본 규칙

- **점수 = 라운드 1~3, 70턴 누적 데미지.** 적은 HP 0 이하여도 죽지 않음(음수 HP 허용).
- 70턴은 R1부터 연속 누적. **R1/R2는 적 전멸(데미지)로만 전환**, R3는 끝까지.
- 라운드 전환: 사망 모션 **1초** 경과(쿨 −1초) 후 다음 라운드. 라운드 진입 턴은 0턴(스킬턴, 턴 미소모).
  진입 스킬은 **새 라운드의 신선한 적**에게 시전되며, 그 스킬이 적을 전멸시킬 때만 다음 라운드로 연쇄한다.
  데미지 없는 버프/디버프 진입 스킬(장비강화 등)은 라운드를 못 깨므로 거기서 끊기고 평타로 진행.
  (구 버그: 죽은 라운드에 버프/디버프를 공짜로 한 번 더 시전 후 전환 — 2026-06-10 정정, `TransitionToNextRound`)
- **회복 스킬 게이팅**: 살아있는 아군이 전부 HP 100%면 회복 스킬(회복 출력 있고 직접 피해 없음, 예: 리나 행진가)은
  발동되지 않고 **다음 예약/우선 스킬**이 나간다(Hold 아님). 빔 후보·자동 선택·플랜 모두 이 게이트 적용.
- 선공: 팀 총 속공 합 높은 팀, 동률 랜덤.
- 스킬턴: 0턴부터 2턴(평타 2회)마다, 선공/후공 교대로 주도권. 턴 미소모, 버프/DoT 턴 차감 없음(쿨만 감소).
- 기본공격(턴 소모): 양팀 통합 속공 내림차순, 동률은 자리번호 낮은 쪽 먼저.
- **진형 효과는 아군에만 적용**(적 진형 없음). 자리 = 공격 순서 + 전/후열 보너스.

### 8.2 시간 모델 (정수 초, 쿨 = 경과 시간만큼 감소)

| 행동 | 소요시간 |
|---|---|
| 기본공격(평타) | 2초 |
| 1스킬 | **3초** |
| 2스킬(컷신) 등 | **4초** |
| DoT 처리 | 1초 (있을 때만) |
| 스킬턴 자체 | 0초 |
| 라운드 사망모션 | 1초 |

(2026-06 실측 보정값. 구 문서의 "1스킬 4초/2스킬 5초"는 폐기.)
약점공격 시 평타 −0.5초 단축은 수치 미확정으로 **보류**(코드 주석 처리).

### 8.3 타겟팅

- **약점 대상 = 최저 HP 적**, 동률이면 자리(Position) 낮은 순. **R3는 항상 보스.**
- 약확 100% 이상이면 단일 타격을 전열 보스로 고정(잡몹 쿨감 패시브 회피).
- 도발 중인 아군이 있으면 적의 공격이 도발자 우선(단일=도발자, 광역=도발자 포함).

### 8.4 치명·약점·감쇄

- 아군 → 적: `ExpectedCritWeak = true` — **치확/약확 100% 가정 금지, 확률 기댓값** (`E[치명]=1+pc(치피/100−1)`).
  - **스킬 내장 치확/약확도 확률에 합산**: `pc = (CritChance + skillBonus.Cri)/100`, `pw = (WeakChance + skillBonus.Wek)/100`.
    예) 파스칼 파괴의거인 6초월 `Bonus.Cri=100`(확정치명) → 이미 100%라 추가 치확 기어는 기댓값 0(옵티마이저가 치피/공%로 재배분).
    이게 빠지면 시뮬·옵티마이저가 확정치명 영웅을 과소평가해 치확 기어를 낭비 배분함.
- 잃은HP 비례는 대상 **실제 잔여HP%** 입력(음수 HP면 풀보너스).
- **공성전 감쇄는 곱연산 후처리**: `최종 = raw × (1−물마감쇄%) × (1−타겟수감쇄%)` (합연산이면 180%↑에서 음수가 되므로).

### 8.5 적 메커니즘 (구현 현황)

| 메커니즘 | 상태 | 내용 |
|---|---|---|
| 광폭화 | ✅ | `HasEnrage` 보스만(친위대 등 제외). 주는 피해 30턴 +50% / 40턴 +100% / 50턴 +150% / 55턴 +200% / 60턴 +500%, 적→아군 피해에만 곱연산 |
| 반격 (제이브, 금) | ✅ | 피격 hit당 25%: 아군 3명에 물리 60% [치확100·치피+500], 소요 3초(→아군 쿨 동반 감소). 옵티마이저 탐색 시 OFF, 최종 점수 ON |
| 죽음의 경계 (델론즈, 목) | ✅ | 아군 사망 시 직격 피해 4회 무효(DoT 제외) |
| 죽음의 일격 (델론즈) | ✅ | 단일 물리 300%×5, OnKillRecast(처치 시 재시전) |
| 방어 준비 (루디, 월) | ✅ | 방어력×100 보호막[5턴], 점수 미집계, 버프해제로 제거 |
| 생명력 전환 (루디, 월) | ✅ | HP 90→80→70% 전환 + 기절[3턴] |
| 챈슬러→스파이크 (토) | ✅ | 공격력 최고 적에게 치확100%·치피+500%[5턴] 부여(적→아군 버프 모델). 미해제 시 전멸급 — 버프해제 필수 기믹 |
| 즉사 (크리스, 일) | ✅ | 매 행동 현재HP 20% DoT + 만료 턴 사망 + 2중첩 즉시 사망. 면역 보유 시 부여 자체가 차단 |
| 피격 쿨감 (일) | ✅ | `SiegeHitCdReduce` — 몹 피격 시 공격력 최고 적 쿨 N초 감소 |
| 적 진영 피해면역 (화 R3) | ✅ | `EnemyImmunityTurns` — 관통 스킬만 통과 |
| R3 룩·챈슬러 피해1 고정 (일) | ✅ | `SiegeDamageCapToOne` — 시스템 설정(패시브 아님). R3 친위대는 피해 1만 받음 → 보스(크리스)에게만 유효타. 약점공격(약확100%↑) 집중·광역기 외엔 크리스 직타 불가. 버프해제·턴제버프감소로 못 풂 |
| 적 실명(Blind) | ✅ | 실명 중 기본공격 100% 빗나감(스킬은 적중), 평타 1회당 1턴 차감 |
| 제이브 반격 화상 DoT | ❌ | 직격만 구현, 틱뎀 0 |
| 적→아군 DoT 일반 | ❌ | 직격만 반영 |

### 8.6 아군 측 메커니즘

- **도발 + 타입 피해면역** (라이언 강자사냥): 자신 도발[2턴] + 물리 피해면역[2턴] → 델론즈 단일기 유도 후 0뎀.
- **평타 발동형 쿨감** (라이언): 평타 Effects의 `Cooldown_Reduction` — 자신+공격력 최고 아군 쿨 9초 감소.
- **EagleClaw 스택** (타카): 평타 2회마다 또는 스킬 발동 시(공격 액션당 1회) 적 3명(R3=보스 포함)에 +2스택.
  최대 8스택, 스택당 취약 4%·받는물리피해 3%. 라운드별 카운터 리셋.
- **보호막**: `Shield_AtkRatio` 등으로 생성, 받는 피해를 흡수·소비. **HP 임계 자힐**: HP 임계 통과 시 비례 회복.
- **생존 판정(SurvivalResolver)**: 영멸 시 전부 봉인 → 불굴/불사(피격/턴 무적) → 권능(현재HP 이상 피해 시 1회 생존+회복/보호막) → 부활(1회) → 사망. 패시브 우선, 장신구(반지)는 빈 슬롯만 보강.
- **버프 타게팅 2-패스**: 1차 시뮬(raw)로 캐릭별 실제 누적딜(DamageWeight)을 구하고, 2차에서 "공격력/딜 최고 아군" 대상 버프(비스킷 장비강화, 라이언 쿨감)를 실딜 1위에게 배분.

### 8.7 진단 도구

- `DiagSkillName(s)`: 특정 스킬 타격 시 입력 전 구성요소(FinalAtk/버프/디버프/계수) 턴별 덤프.
- `DiagAllyStats`: raw 공격력 1위 vs 실딜 1위 불일치 경고(버프 수신자 검증).
- 결정값(치명+약점 확정) 병기 출력 — 실측 엑셀(공성전_요일.xlsx) 플로팅 데미지와 1:1 대조용.
- `tools/SiegeDayLogs` 인자 "전투로그"로 턴별 로그 덤프.

---

## 9. 옵티마이저

### 9.1 EquipmentOptimizer (단일 캐릭터)

1. **세트 조합**: 4세트 + 2+2세트(무기세트≠방어구세트) 전수.
2. **메인옵션**: 무기×2·방어구×2 후보 순회, DPS 옵션 필터(공%, 공, 치확, 치피, 약확).
3. **서브옵션 그리디**: 부옵 4종을 1티어로 깔고, 티어업 5회를 "1티어당 데미지 증가 최대" 옵션에 배분
   (슬롯당 최대 6티어). 치확/약확은 파티 바닥값 기준 100% 캡 도달 시 가치 0 처리.
4. **장신구**: 등급(6→5→4성) × 메인 × 세공(6성만) 순회.
5. 평가: 대표 스킬(최고 배율) 1타를 `ExpectedCritWeak=true` 기댓값으로 계산.

공성용 `FastOptimize`: 세트×메인을 무부옵으로 1차 선정 후 그 구성에만 부옵·장신구 최적화.
역할 기반 제약(딜러=DPS 기어 / 면역 패시브 보유=생존 기어 / 기타=혼합).

**치확/약확 floor 캡 (`FloorFirstGear`)**: `PartyBuffCritWeak`가 딜러가 버스트 윈도우에 받는 파티 치확/약확 합
(비스킷 장비강화 약확54 등)을 floor로 계산해, `DisplayStats + floor ≥ 100`이면 그 이상 치확/약확 기어를 거절 →
부옵이 치피/공%로 재배분. SiegeBatchAllDays는 `FloorFirstGear = true`(2026-06-10). floor는 기어 부옵만 막으므로
base 치확/약확이 이미 높으면 100% 미만으로 못 내릴 수 있음(그 캐릭 기어 약확만 0으로).
부옵 캡 판정엔 floor 외에 **대표 스킬 내장 치확/약확(`capBonus`)**도 합산 — 파스칼 확정치명처럼 스킬이 이미 치확을
보장하면 치확 부옵 자체를 거절. (데미지 평가 자체는 DamageCalculator가 `skillBonus.Cri/.Wek`를 직접 반영하므로 일관됨.)

### 9.2 SiegeOptimizer (공성전)

```
팀 조합 C(N,5) 전수  ×  진형 3종(기본/밸런스/보호)  ×  후열 비트마스크 전수
  → 영웅별 장비 자동장착(FastOptimize, 역할 기반)   [고정 시드 777, 결정론]
  → 자동 로테이션 점수로 1차 평가 (BuffFirstAuto: 파티버프 셋업을 딜러 핵보다 먼저)
  → 빔서치 후보 선정: ① 자동로테 1위 ② 메인딜러-후열 ③ 상위 N딜러 전원-후열 (+ 전문가 시드 로테)
  → RotationBeamSearch (빔폭 10 · 깊이 36, 결정점별 Hold 포함 분기, 경로마다 70턴 풀시뮬)
  → 교차수분: 각 후보의 우승 로테를 서로의 config에 교차평가, 무회귀 채택
  → 생존반지 후처리(ApplySurvivalRings, 최대 5회 반복) + 채택 시 로테 재최적화(crossPlans 재평가 포함)
```

- **선택 기준(IsBetterPick, 전 구간 통일)**: ① raw 딜(`TotalScore`) 우선(허용오차 ±1.0) ② 동률일 때만
  RankScore(= 실제딜 − 사망 페널티) 높은 쪽. 산발적 사망 페널티가 고점 빌드를 가리지 않게 raw로 선정하되,
  생존반지 채택 등 생존 판단에 RankScore를 보조로 사용.
- 빔서치 중 반격은 0%(OFF)로 탐색(RNG 비의존), 최종 점수·기어·생존반지 판단은 25%(ON).
- 알려진 한계: 탐욕적 빔이 "셋업(디버프/버프) → 딜러 버스트" 지연보상 정렬을 놓침 → 전문가 시드 로테로 보완 중.
- **실행 표준 프로필**: 12초월 단일 · 전용장비 전설 조율 탐색 · 펫 모공 76.

---

## 10. 데이터 DB 구조

### 10.1 CharacterDB

전설/희귀 111명 등재. `#region 등급-역할-물리/마법` 분류, `AttackType` 명시(물리는 기본값 생략 가능).
효과는 전부 통합 `Effects` 리스트(4절). 스탯은 BasicStatDB 조회(캐릭터 선언에 미포함).

### 10.2 EnemyDB / SiegeDayDb / SiegeBossSkillDb

- **공성 보스 7명**(요일별): 루디(월)·아일린(화)·레이첼(수)·델론즈(목)·제이브(금)·스파이크(토)·크리스(일).
  요일별 감쇄(물리/마법/1인기/3인기/5인기, 예: 토 마법90·단일70·광역90)와 고유 메커니즘 플래그
  (`HasEnrage`, `OnAllyDeathNullifyHits`, `Counterattack`, `SiegeHitCdReduce` 등)를 보유.
- **공성 몹(룩·챈슬러)**: R1/R2 스탯은 요일 불변(몹·라운드별), **스킬만 요일별**. R3는 스탯·스킬 모두 요일별.
  감쇄는 보스와 동일 부여. 적 스킬은 `SkillPriority`(쿨 충족 중 최고 순위 1개) 소비.
- 라운드 구성: `SiegeStages[요일].Waves`(R1~R3), R3 `StageEnemy.IsBoss=true`.
- 그 외: 레이드 보스(스택형 방어 증가), 강림 보스 등.

### 10.3 EquipmentDB

- 공통 기본: 무기 공304 / 방어구 방189·생1079.
- 메인옵션: 슬롯별 풀(무기=공%·공·치확·치피·약확·생%·생·방%·방·효적 / 방어구=공%·공·생%·생·방%·방·효저·받피감·막기).
- 부옵션: 1티어 기본값 × 티어(슬롯당 1+5강화 = 최대 6티어, 부옵 4슬롯). 메인·서브가 변수명 공유(의도된 설계).
- 세트(2/4): 선봉장(공) · 추적자(약확/약피) · 성기사(생/회복) · 수문장(막기) · 수호자(방/효저) · 암살자(치확/방무) ·
  **복수자(피증15/30 + 보스피증40)** · 주술사(효적) · 조율자(효저).

### 10.4 Accessory / AccessoryEffectDb

- 장신구 = 등급(4/5/6성) 보너스 + **메인옵션** + **세공(SubOption, 6성만, 다른 장신구의 옵션 1개·스탯 없음)**.
- 딜 옵션 풀(6성): 보스피증12(토벌)·치확10·약확12·피증6·1-3인기10(공성)·4-5인기10(섬멸) 등. **치피는 장신구에 없음**(무기·방어구 전용).
- 효과형 반지(AccessoryEffectDb, 32종×성급): 부활/불사/권능(생존, SurvivalResolver 소비), 보호막/자힐, CC·상태이상 부여, 스탯 상승. 생존 반지는 공성 옵티마이저 후처리에서 사용.

### 10.5 ExclusiveWeaponDb / PetDB

- 전용무기: flat 공격력 247(타입별) + 조율 4슬롯(모든공격력/피해증폭/약점피해/치명피해 등).
  미등록 캐릭은 "전설 4슬롯 모든공격력(+48%)" 폴백.
- 펫: 성급별 기본 스탯 + 성급별 스킬 버프(예: 윈디 6성 공12%+보스취약26%) + **스킬강화 0~+3**(6성 한정,
  `EnhanceBonus`가 0이 아닌 필드만 Override — 성급값에 가산이 아니라 절대값 대체).
  펫 잠재 옵션(공/방/생%)은 입력값으로 받아 2.1절 ④(공·방) / ②(생) 경로로 적용.

---

## 11. 도구(tools)와 웹 연동

| 도구 | 역할 |
|---|---|
| DmgCheck | 단일 스킬 1타 데미지 단계별 분해(검증 하니스) |
| CompileCheck | 공유 코어 컴파일 + 풀시뮬 스모크 테스트 |
| SiegeBatchAllDays | 월~일 전 요일 탐색 배치 실행, 요일별 JSON/TXT 산출 |
| SiegeFixedTeamOpt | 고정 팀 + 기어·진형·로테만 탐색 |
| SiegeManualSim | 수동 세팅·로테이션 강제 시뮬 |
| SiegeDayLogs | 실측(엑셀) 세팅을 RotationPlan으로 잠가 재현, "전투로그" 턴별 덤프 |
| SiegeRyanBackExp | 자리(전/후열) 실험 하니스 |
| TuningCompare | 전용무기 조율 A/B 데미지 비교 |
| SenaDataExport | CharacterDB → `web/src/data/characters.json` (웹 데이터 파이프라인) |
| SiegeApi | 공성전 웹 백엔드 (ASP.NET minimal API) |

**SiegeApi**: `GET /api/siege/days`, `GET /api/siege/pets`, `POST /api/siege/optimize`(캐시 히트 시 즉시,
미스 시 백그라운드 잡 + `GET /api/siege/job?id=` 폴링), `POST /api/siege/prefill`. 웹(`/siege`)이 fetch로 호출.

**웹(web/)**: Next.js 16 + React 19 + Tailwind v4. Turbopack 한글경로 패닉 → **webpack 고정**.
데이터 갱신: `cd tools/SenaDataExport && dotnet run`.

---

## 12. 검증 이력과 알려진 한계

### 12.1 데미지 공식 검증 (통과)

| 케이스 | 오차 |
|---|---|
| 에스파다 (Mark_Purify + 스택소모 복합) | 0.003% |
| 루리 (약점 추가피해) | 0.9% |
| 타카 (조건부 추가피해) | 1.6% |
| 라이언 광풍참 (잃은HP 비례) | ~0% |
| 공성 토요일 보스 1타 (DmgCheck, 전체 디버프 정렬) | ~1% (290,500 vs 실측 285,884~294,630) |
| 공성 토요일 풀시뮬 70턴 누적 | 실측 ~9M의 95% (8.51M) |

핵심 교훈: **공식(DamageCalculator)은 정확**하다. 풀시뮬 오차의 주범은 항상 버프/디버프의
*집계·타이밍·대상 선정*이었다 (디버프 출처별 Add, 버프 수신자, 스택 귀속, per-target 조건 게이팅).

### 12.2 회귀 테스트 방침

위 4명 골든 케이스는 옵티마이저 확장 시 자동화 패턴으로 통합 예정. 코어 공식 변경 시에만 수동 회귀 우려.

### 12.3 미구현 / 보류 (코드 확인 기준)

- 적→아군 DoT 틱뎀(제이브 반격 화상 포함) — 직격만 반영.
- 약점공격 시 평타 0.5초 단축 — 수치 미확정, 주석 보류.
- 메인 시뮬 쪽 런타임 미소비 효과 다수(부활/권능/무효화 등은 **공성 시뮬·SurvivalResolver에는 구현됨**),
  열 한정 타겟, 쿨증 디버프, 상태이상 해제 폭발, per-turn regen 등 — 모델·데이터만 존재(docs/FOLLOWup 참조).
- 상태이상 초월 확률 상승의 base+초월 이중 적용 가능 데이터 — 런타임 dedup 또는 데이터 정리 필요.
- 펫 스킬강화 수치 데이터 일부 미입력(파이프라인은 완성).
- 빔서치의 셋업→버스트 지연보상 정렬 한계 — 전문가 시드 로테로 보완, MCTS/콤보 템플릿은 후보.

---

## 13. 부록: 구 문서 이관표

| 구 문서 | 처리 |
|---|---|
| `damage_test_results.md` | **폐기 권고** — 2026-01-29 시점 기록. "피증 곱연산" 등 폐기된 가설 포함(현행은 합연산, 5.1절). 실측값은 12.1절로 흡수 |
| `system_update.md` | **폐기 권고** — 2026-04-21 웹 전환 계획. 완료/구식 |
| `.claude/CLAUDE_MEMORY.md`, `.claude/log/*` | 작업 일지 — 시스템 서술은 본 문서로 흡수. 기록 보존용으로 유지 가능 |
| `SIEGE_FORMULA_CALIBRATION.md` | 검증 **과정 증거** 기록 — 보존 권장(결론은 12.1절에 요약) |
| `docs/SIMULATION_PLAN.md` | 구현 완료된 계획 — 폐기 권고(규칙은 8절로 흡수) |
| `docs/SIEGE_POSITIONING_PLAN.md` | 완료된 실험 기록 — 보존/폐기 자유(결론: 자리 휴리스틱·교차수분은 9.2절) |
| `docs/PHASE5_MAGIC_ATTACK_MIGRATION.md` | 완료된 마이그레이션 — 폐기 권고(규칙은 2.2절로 흡수) |
| `docs/FOLLOWUP.md` | **유지** — 미구현 후속 작업 목록(12.3절이 요약 참조) |
| `memory/*.md` | 유지 — 세션 메모리(설계 의도·결정 기록) |
| `CLAUDE.md` | 유지 — 작업 지침 + 본 문서 참조로 축소 권장 |

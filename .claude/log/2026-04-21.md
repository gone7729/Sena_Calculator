# 2026-04-21 작업 로그

## Q: coord 시스템 버전 업그레이드 적용

사용자: coord 시스템 버전 업그레이드 적용해줘 (https://github.com/gone7729/coord-template)

### 현황 파악
- `.coord` 서브모듈 v0.12 → origin/main v0.18 (17 커밋 뒤)
- 로컬 수정: `scripts/start-bot.sh` (python3 → python, Windows 호환)
- 미추적: `coordination/config.yml`, `hotfixes.md`, `token-budget.md`, `.coord-version`

### 결정
- 옵션 B 선택: 커스터마이즈 폐기하고 v0.18 그대로 사용

### 수행
1. `git -C .coord checkout -- scripts/start-bot.sh` (로컬 수정 폐기)
2. `git -C .coord pull --ff-only` → v0.18 fast-forward
3. 부모 repo에 서브모듈 포인터 커밋: `ec73afb chore: upgrade .coord submodule v0.12 → v0.18`

---

## Q: Discord 봇 사용 안 함, 로컬 인박스로 운영

사용자: 디스코드 사용안하고 로컬에서 인박스 명령어로 사용할거야 지금은

### 조치
- 프로젝트 메모리에 기록 (`memory/project_coord_workflow.md`)
- 향후 봇 재시작/Discord 관련 안내 생략

---

## Q: 웹 앱 신규 프로젝트 — 캐릭터/장비/추천/턴제 옵티마이저

사용자: 현재 DB 데이터로 간단하게 웹에서 캐릭터 정보와 템 등을 가지고 영웅별 템추천 던전별 영웅 추천 그리고 추가하려던 턴제 최적화 빌드 등 제공하려해

### 스택 결정
- **프레임워크**: Next.js (React)
- **배포**: Vercel
- **언어**: TypeScript
- **데이터 레이어**: JSON-only (Supabase 제외)
  - 이유: 게임 데이터는 정적 (패치 시에만 갱신), 로그인 필요 기능 없음, Vercel CDN 캐싱 활용
  - 향후 user-specific 기능(프리셋/즐겨찾기/공유) 필요 시 Supabase 추가 검토

### DB 이전 전략
- **(a) C# DB 클래스 → JSON 자동 export 스크립트** 확정
- 대안 (b) 수기 TS 마이그레이션, (c) .NET API Route 는 제외
- 기존 WPF 계산기와 데이터 정합성 유지

### 미결정 (head plan 단계에서 재확인)
- 계산 엔진 (DamageCalculator / BattleSimulator / EquipmentOptimizer) 포팅 범위
  - MVP: 정보 열람 + 단순 추천만
  - Full: 시뮬레이터/옵티마이저까지 TS 포팅
  - 권장: 단계별 진행 (MVP → 점진 확장)
- 웹 앱 디렉토리 위치: `web/` 서브디렉토리 vs 별도 repo

### inbox 등록
- 파일: `.coord/coordination/inbox/2026-04-20-205220.md`
- 커밋: `54c9920` (초안), `847277d` (JSON-only 결정 반영)
- coord-template repo 에 push 완료

---

## 📋 오늘 작업 요약

### 완료
- ✅ `.coord` 서브모듈 v0.12 → v0.18 업그레이드
- ✅ coord 운영 모드 전환: Discord 봇 OFF → 로컬 인박스 전용
- ✅ 신규 프로젝트 inbox 등록 (Next.js 웹 앱 + JSON-only)
  - 스택/배포/데이터 레이어/DB 이전 전략 확정

### 진행 중 / 대기
- ⏳ inbox 처리 — head worktree 미설치 상태
  - 처리 방식 결정 필요: head worktree 구축 vs 메인 세션 직접 작업
- ⏳ 웹 앱 디렉토리 위치 결정 (`web/` vs 별도 repo)
- ⏳ 계산 엔진 포팅 범위 결정 (MVP / Full)

### 다음 작업 후보
1. Next.js 프로젝트 scaffolding (`create-next-app`)
2. C# DB → JSON export 스크립트 작성
3. 기본 페이지 (캐릭터 목록/상세)
4. 장비/던전 추천 UI
5. (선택) 계산 엔진 TS 포팅 + Vercel 배포